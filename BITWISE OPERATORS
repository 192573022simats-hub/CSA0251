#include <stdio.h>
int main() {
    int a= 31, b= 14;
    printf("a & b = %d\n", a & b);  
    printf("a | b = %d\n", a | b); 
    printf("a ~ b = %d\n", a ~ b); 
    printf("a ^ b = %d\n", a ^ b);  
    return 0;
}
